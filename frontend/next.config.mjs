/** @type {import('next').NextConfig} */
const nextConfig = {
  env: {
    API_BASE_URL: "https://pdf-editor-api.onrender.com",
  },
  // output: "export",
  // assetPrefix: "./",
  // basePath: "/",
};

export default nextConfig;
